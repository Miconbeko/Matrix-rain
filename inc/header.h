#ifndef HEADER_H
#define HEADER_H

#include <stdio.h>  
#include <stdlib.h>  
#include <ncurses.h>
#include <unistd.h>
#include <time.h>

struct rain {
    int color;
    int length;
    int speed;
    int direction;
    char symbol;
}      rain;


#endif

